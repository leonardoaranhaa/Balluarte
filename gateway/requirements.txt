# Fase 0 — validação da premissa técnica.
# Fase 1 — motor de política.
# Fase 2 — trilha de auditoria.
# Fase 3 — integração do proxy.
#
# Versões fixadas: LiteLLM e Presidio publicam com frequência, e uma fase só
# vale como evidência se puder ser re-rodada no mesmo estado.
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
litellm==1.97.0
pyyaml==6.0.3
psycopg[binary]==3.3.4
cryptography==48.0.1
starlette==1.6.0
uvicorn==0.52.3
httpx==0.28.1
pytest==9.1.1

# Só para o teste de compatibilidade de API, que roda o SDK oficial contra o
# gateway trocando apenas a base_url.
anthropic==0.122.0

# Modelo de linguagem do spaCy, instalado à parte:
#   python -m spacy download pt_core_news_sm
